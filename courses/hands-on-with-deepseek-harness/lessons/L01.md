---
id: course-deepseek-harness-l01
type: course
title: "Lesson 1: Reproduce the failure, read it back from the trajectory"
summary: "Run a harness-composed agent on the course folder, make the failure objective with verify.py — then read the append-only trajectory to see exactly what the agent did."
lang: en-US
content_version: 1
status: reviewed
reviewed_on: 2026-09-13
hints:
  - title: "What this checkpoint tests"
    body: "That you can reproduce the failure before fixing it: `python verify.py starter --expect-failure` must print the named failing tests — whether you ran it or the agent's shell tool did, the command is the evidence."
  - title: "Everything is a plugin"
    body: "In DeepSeek Harness the agent is composed, not built-in: model, tools, session storage, and the loop itself are plugins wired in `cordis.yml`. If a behavior is missing, the answer is usually a plugin — not a flag."
  - title: "The trajectory is your record"
    body: "Every step lands in an append-only session/trajectory log you can inspect later. Reading it back is how you verify what the agent actually did — not what it said it did."
  - title: "When you are done"
    body: "You can say which nine behaviors the suite proves and why the starter cannot fake them. `python verify.py progress` then shows this checkpoint's claim code."
---

# Lesson 1: Reproduce the failure, read it back from the trajectory

## Objective

By the end of this lesson you have a working DeepSeek Harness session
pointed at the course folder, you have run the course's objective
verification command, and you can explain what "the starter fails" means
in test names — confirmed against the session trajectory. No code is
changed in this lesson.

## Why this lesson exists

Most AI-written Python dies the same way: the demo input works, the real
input arrives, and the script crashes on row 3 — or worse, quietly
produces a wrong number. The fix does not start with a better prompt. It
starts with turning "it's broken" into a command that anyone can run and
get the same answer. DeepSeek Harness adds a second layer: the agent you
run is *composed* — model, tools, storage, and loop are plugins wired
together in `cordis.yml` — and every step it takes lands in an append-only
trajectory log you can inspect, search, and replay. "Trust me" becomes
"here is the trace."

## Warm-up (2 minutes)

Open `starter/report_tool.py` and read it top to bottom. It looks
reasonable: functions with docstrings, type hints, a CLI. This is exactly
what AI-generated code looks like — clean surface, missing behavior. Now
open `TASK.md` and keep it next to you.

## The lesson

With a harness session running in the course folder, tell the agent:

**"Read COURSE.md — it is the teaching contract for this folder. Then read
TASK.md and starter/report_tool.py. Do not change any file. Tell me which
behaviors the tests expect that the starter does not have."**

While it reads, run the objective verification — yourself in a terminal,
or through the agent's shell/sandbox tool:

```bash
python verify.py starter --expect-failure
```

You should see the starter reproduce four real-world failure classes,
mapped to test names:

| Failing test | Real-world meaning |
| --- | --- |
| `test_load_json_records_returns_list_of_dicts` | the tool only handles CSV; the API scenario crashes |
| `test_unsupported_suffix_raises_value_error` | an `.xlsx` upload becomes a confusing crash instead of a clear rejection |
| `test_invalid_records_are_isolated_with_reasons` | one empty cell aborts the entire run — no partial report, no reason |
| `test_group_totals_are_rounded_to_two_decimals` | `0.1 + 0.2` prints as `0.30000000000000004` in your report |
| `test_write_report_creates_missing_parent_directories` | the tool cannot write into a fresh output directory |
| `test_run_scenario_writes_report_file`, `test_main_prints_summary_and_returns_zero` | end-to-end consequences of the above |

Then run the reviewed implementation:

```bash
python verify.py solution
```

Both commands exit 0 — but they mean opposite things. The first proves the
starter is *correctly unfinished*; the second proves the target is
reachable.

Now do the harness-native step: inspect the session trajectory. Find the
steps where the agent read `TASK.md`, ran (or was told about) the verify
commands, and produced its summary. The trajectory is append-only — it is
the ground truth of what happened, and it is what makes this agent's work
auditable later.

## Exercise

Pick the scenario skin you most identify with (`excel-report`,
`data-monitor`, or `api-tool`), open its data file, and find the rows that
will be rejected. Write down, before running anything, which test each bad
row will trigger. Then check yourself against the table above — and find
the trajectory step where the agent reached the same conclusion.

## Checkpoint

Run `python verify.py starter --expect-failure` and
`python verify.py solution`. You pass this lesson when you can answer,
without looking:

1. Which four behaviors separate starter from solution?
2. Why is "expected failure" a success condition, not a contradiction?
3. Where in the trajectory can you prove the agent actually read
   `TASK.md`?

## Expected evidence

The transcript of both commands, the trajectory steps you found, and your
three answers. Keep them; Lesson 4 will ask for them again.
